kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      # Lookout UI
      - containerPort: 30000
        hostPort: 30000
        protocol: TCP
      # Armada Server HTTP/REST
      - containerPort: 30001
        hostPort: 30001
        protocol: TCP
      # Armada Server gRPC
      - containerPort: 30002
        hostPort: 30002
        protocol: TCP
      # Binoculars HTTP/REST
      - containerPort: 30003
        hostPort: 30003
        protocol: TCP
      # Binoculars gRPC
      - containerPort: 30004
        hostPort: 30004
        protocol: TCP
